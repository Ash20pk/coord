# Console sign-in. Both are safe in a browser bundle: the anon key is public by
# design and every table it can reach is behind row-level security.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
